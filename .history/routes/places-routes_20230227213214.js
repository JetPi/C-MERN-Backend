const express = require("express");
const bodyParser = require("body-parser");

const router = express.Router();

route.get("/:pid", () => {});

route.patch("/:pid", () => {});

route.delete("/:pid", () => {});
