const express = require("express");
const bodyParser = require("body-parser");

const router = express.Router();

router.get("/:pid", () => {});

routr.patch("/:pid", () => {});

router.delete("/:pid", () => {});
